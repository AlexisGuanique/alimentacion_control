version: "3.9"

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: nutritrack_backend
    restart: unless-stopped
    ports:
      - "8010:8000"
    env_file:
      - .env
    volumes:
      - db_data:/app/database.db

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: nutritrack_frontend
    restart: unless-stopped
    ports:
      - "3010:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://34.29.59.97:8010
    depends_on:
      - backend

volumes:
  db_data:
    driver: local
